/* globals $ */
'use strict';

angular.module('hipokApp')
    .directive('hipokAppPager', function() {
        return {
            templateUrl: 'scripts/components/form/pager.html'
        };
    });
